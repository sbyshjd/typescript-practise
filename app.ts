console.log("code--------------");

function add(num1: number, num2: number) {
  return num1 + num2;
}

const num1 = 5;
const num2 = 3.7;

enum Role {ADIM, READ_ONLY, AUTHOR}

console.log(add(num1, num2));
